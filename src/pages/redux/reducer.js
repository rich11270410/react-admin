import React, { Component } from 'react'

const iniHeattitle = "首页"

function header(state= iniHeattitle,action) {
  switch (action.type) {
    case value:
      
      break;
  
    default:
      break;
  }
  
}